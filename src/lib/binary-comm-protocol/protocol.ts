/**
 * Binary Communication Protocol
 *
 * Primary file responsible for setting up the protocol for websocket communication in binary format.
 * Encoding and Decoding is based off this predefined protocol in collboration with the websocket server.
 **/

import { MessageValue } from "@/type/editor";

/*
 * Encode Message
 */
// function encodeMessage(): Buffer { }
//

/*
 * Decode Message
 */
function decodeMessage(message: ArrayBuffer): MessageValue {
  return [""];
}
